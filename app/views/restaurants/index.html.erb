<h1>Restaurants</h1>


  <% @restaurants.each do |restaurant| %>
    <%= restaurant.name %>
    <p><%= link_to "Show details", restaurant_path(restaurant), class: 'btn btn-info' %><p>
  <% end %>

  <div class = 'align_button'>
    <%= link_to "Add a New Restaurant", new_restaurant_path, class: 'btn btn-primary' %>
  </div>
